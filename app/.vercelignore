# One Vercel project: static Vite build + the /api Function (which imports server/src).
# node_modules is installed by Vercel at build time; dist is generated, not uploaded.
node_modules
dist
*.db
*.db-journal
*.db-wal
*.db-shm
.env
*.log
